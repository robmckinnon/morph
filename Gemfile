source 'https://rubygems.org'

group :test do
  gem 'rspec'
  gem 'guard-rspec'
end

group :development do
  # gem 'fattr'
  # gem 'arrayfields'
  # gem 'map'
  # gem 'metrical'
  gem 'echoe'
end

gem 'active_support' if RUBY_VERSION >= '1.9'
gem 'activesupport' if RUBY_VERSION < '1.9'

gem 'rake' # required for travis builds
