source 'https://rubygems.org'

group :test do
  gem 'rspec'
  gem 'guard-rspec'
end

group :development do
  # gem 'fattr'
  # gem 'arrayfields'
  # gem 'map'
  # gem 'metrical'
  gem 'echoe'
end


if RUBY_VERSION < '1.9'
  gem 'activesupport'
  gem 'fastercsv'
else
  gem 'active_support'
end

gem 'rake' # required for travis builds
